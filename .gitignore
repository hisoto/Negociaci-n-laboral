# ── Artefactos de R / RStudio ────────────────────────────────────────────────
.Rhistory
.RData
.Rproj.user/
Rplots.pdf
/-MacBook Air de Héctor (2).Rhistory
/master_negociaciones-MacBook Air de Héctor (2).R
/master_negociaciones.R
/Negociación laboral-MacBook Air de Héctor (2).Rproj

# ── Claude Code config (privado) ─────────────────────────────────────────────
.claude/
CLAUDE.md

# ── Python cache ─────────────────────────────────────────────────────────────
scripts/__pycache__/

# ── Insumos confidenciales ───────────────────────────────────────────────────
# Tarjetas semanales CIS (PDFs de la STPS) y los CSVs derivados de su extracción
inputs/cis_pdf/
inputs/cis_csv/
# Documentos Word de huelgas vigentes que envía la DT
inputs/word_huelgas_vigentes/
# Catálogo INEGI crudo (se descarga, no se edita)
inputs/catalogos/*.dbf

# ── Entregables y material archivado ─────────────────────────────────────────
# Informes finales en Word/PDF
outputs/
# Material heredado, borradores viejos y respaldos: se conservan en disco,
# no en el repositorio
_archivo/
graphs/_archivo/
